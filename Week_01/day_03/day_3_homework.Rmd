
```{r}
library(tidyverse)
```
```{r}
library(janitor)
```

```{r}
movies <- read_csv('6_homework_cleaning_data/data/movies.csv')

movies

```

```{r}
head(movies)

tail(movies)

nrow(movies)

ncol(movies)
```
```{r}
movies %>% 
  filter(runtime < 10)
```

```{r}
movies_selected <- movies %>% 
  select(title, runtime, budget) %>% 
  
movies_selected
```
```{r}
filter(movies_selected, runtime < 10)
```

```{r}
movies %>% 
  summarize(na_count = sum(is.na(movies_selected)))
```

```{r}
movies %>%
  select(runtime) %>% 
  arrange(runtime) %>% 
  filter(runtime == 0) %>% 
  mutate(runtime_null = na_if(runtime < 10, 'NA')) %>% 
  sum()


```

```{r}
movies_selected %>% 
  
```

```{r}
movies_inputed <- movies %>% 
  mutate(runtime = coalesce(runtime == 0, 'unavailable'))
```

```{r}
movies %>%
  mutate(runtime = coalesce(runtime))
```

```{r}
summary(movies, runtime < 10)
```

